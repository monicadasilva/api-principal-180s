(ns api-principal.api-principal-180s-test
  (:require [clojure.test :refer [deftest is testing]]
            [api-principal.api-principal-180s :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
